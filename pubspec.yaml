name: data_size
description: '🔟 Customizable data size (bit & byte) formatting for interfacing with the user'
version: 0.2.0
homepage: https://github.com/JonasWanke/data_size

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  meta: ^1.3.0

dev_dependencies:
  extra_pedantic: ^1.3.0
  test: ^1.16.8
  # TODO(JonasWanke): Re-add when test_coverage gets updated.
  # test_coverage: ^0.5.0
